gridGallery({
    selector: "#gallery", // name it
    darkMode: false, // enable dark mode
    gapLength: 10, // space between images
    columnWidth: 300, // column width
    // or "horizontal"
    layout: "vertical",
});